(()=>{'use strict';const SNAP='fotocrm:snapshot:v2';const read=()=>{try{return JSON.parse(localStorage.getItem(SNAP)||'{}')}catch{return{}}};const write=v=>{try{localStorage.setItem(SNAP,JSON.stringify(v))}catch{}};const fmt=iso=>{const d=new Date(iso||'');return Number.isFinite(d.getTime())?d.toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric'}):''};function findShoot(card){const snap=read(),name=(card.querySelector('h3')?.textContent||'').trim(),date=(card.textContent||'').match(/\d{2}\.\d{2}\.\d{4}/)?.[0];return (snap.shoots||[]).find(s=>String(s.clientName||'').trim()===name&&(!date||fmt(s.startAt)===date))||(snap.shoots||[]).find(s=>String(s.clientName||'').trim()===name)||null}async function removeShoot(shoot,card,btn){if(!shoot)return;btn.disabled=true;btn.style.opacity='.65';const y=window.scrollY;let next=null;try{const r=await fetch('/api/crm',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'deleteShoot',data:{},id:shoot.id})});if(r.ok)next=await r.json()}catch{}if(next&&Array.isArray(next.shoots))write(next);else{const snap=read();snap.shoots=(snap.shoots||[]).filter(s=>Number(s.id)!==Number(shoot.id));write(snap)}card.style.transition='opacity .18s ease,height .2s ease,margin .2s ease,padding .2s ease';card.style.opacity='0';setTimeout(()=>{card.remove();requestAnimationFrame(()=>window.scrollTo(0,y))},180)}function handler(e){const btn=e.target.closest?.('button');if(!btn)return;const text=(btn.textContent||'').trim();if(text!=='Удалить')return;const card=btn.closest('.shoot-card');if(!card)return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();const shoot=findShoot(card);if(!shoot)return;const name=shoot.clientName||'эту съёмку';if(!window.confirm(`Удалить съёмку «${name}»? Это действие нельзя отменить.`))return;removeShoot(shoot,card,btn)}document.addEventListener('click',handler,true);})();