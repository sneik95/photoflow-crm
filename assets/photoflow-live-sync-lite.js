(()=>{'use strict';const SNAP='fotocrm:snapshot:v2';const read=()=>{try{return JSON.parse(localStorage.getItem(SNAP)||'{}')}catch{return{}}};const money=n=>Number(n||0).toLocaleString('ru-RU');const norm=s=>String(s||'').trim().toLowerCase();const fmt=iso=>{const d=new Date(iso||'');return Number.isFinite(d.getTime())?d.toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric'}):''};function syncRadar(){const radar=document.querySelector('.risk-radar');if(!radar)return;const grid=radar.querySelector('.risk-grid'),h=radar.querySelector('h2');if(!grid||!h)return;const shoots=(read().shoots||[]).filter(s=>Number(s.paidAmount||0)<Number(s.price||0)),total=shoots.reduce((a,s)=>a+Math.max(0,Number(s.price||0)-Number(s.paidAmount||0)),0),buttons=[...grid.querySelectorAll('button')],balance=buttons.find(b=>/не получено/i.test((b.textContent||'').replace(/\s+/g,' ')));if(!shoots.length){balance?.remove();const n=grid.querySelectorAll('button').length;h.textContent=n?`${n} ${n===1?'сигнал требует':'сигнала требуют'} внимания`:'Всё под контролем';if(!n){let e=radar.querySelector('.radar-empty');if(!e){e=document.createElement('p');e.className='radar-empty';radar.appendChild(e)}e.textContent='Рисков сейчас нет'}return}radar.querySelector('.radar-empty')?.remove();if(balance){const strong=balance.querySelector('strong'),small=balance.querySelector('small');if(strong)strong.textContent=`${money(total)} ₽ не получено`;if(small)small.textContent=`${shoots.length} ${shoots.length===1?'клиент с остатком':'клиента с остатком'}`}const n=grid.querySelectorAll('button').length;h.textContent=n?`${n} ${n===1?'сигнал требует':'сигнала требуют'} внимания`:'Всё под контролем'}function findStat(label){return [...document.querySelectorAll('.stat-card')].find(c=>norm(c.querySelector('span')?.textContent).includes(norm(label)))||null}function syncDashboard(){if(document.querySelector('.day-mode'))return;const snap=read(),shoots=Array.isArray(snap.shoots)?snap.shoots:[],now=Date.now(),processing=shoots.filter(s=>!s.delivered&&!s.archived&&new Date(s.endAt||s.startAt||0).getTime()<now).length,upcoming=shoots.filter(s=>!s.delivered&&!s.archived&&new Date(s.startAt||0).getTime()>=now).length,p=findStat('в обработке'),u=findStat('ближайшие съёмки');if(p){const n=p.querySelector('strong');if(n)n.textContent=String(processing)}if(u){const n=u.querySelector('strong');if(n)n.textContent=String(upcoming)}const panel=document.querySelector('.shoot-list-panel');if(!panel)return;const counter=panel.querySelector('.panel-title-row .panel-actions>span,.panel-title-row>span');if(counter)counter.textContent=`${shoots.length} ${shoots.length===1?'проект':'проекта'}`;for(const card of panel.querySelectorAll('.shoot-card')){const name=(card.querySelector('h3')?.textContent||'').trim(),date=(card.textContent||'').match(/\d{2}\.\d{2}\.\d{4}/)?.[0],exists=shoots.some(s=>String(s.clientName||'').trim()===name&&(!date||fmt(s.startAt)===date));if(!exists)card.remove()}}let queued=false;function sync(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;syncRadar();syncDashboard()})}if(!Storage.prototype.__pfLiteSync){const prev=Storage.prototype.setItem;Storage.prototype.setItem=function(k,v){const r=prev.call(this,k,v);if(this===localStorage&&k===SNAP)sync();return r};Storage.prototype.__pfLiteSync=true}window.addEventListener('storage',e=>{if(e.key===SNAP)sync()});window.addEventListener('pageshow',sync);document.addEventListener('visibilitychange',()=>{if(!document.hidden)sync()});document.addEventListener('click',e=>{const t=(e.target?.closest?.('button,a')?.textContent||'').trim();if(t==='×'||t==='Мои съёмки'||t==='Сдано'||t==='Удалить')setTimeout(sync,120)},true);sync();})();