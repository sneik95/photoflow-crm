(()=>{'use strict';function unlock(){document.querySelectorAll('.day-timeline .pf-time,.day-timeline .pf-label-input').forEach(el=>{el.style.pointerEvents='auto';el.style.position='relative';el.style.zIndex='20';el.removeAttribute('readonly');el.removeAttribute('disabled');if(el.classList.contains('pf-time')){el.setAttribute('type','time');el.setAttribute('inputmode','numeric')}else{el.setAttribute('type','text');el.setAttribute('inputmode','text');el.setAttribute('autocomplete','off')}});document.querySelectorAll('.day-timeline .pf-add-box,.day-timeline .pf-add-fields').forEach(el=>{el.style.pointerEvents='auto';el.style.position='relative';el.style.zIndex='19'})}function focusInput(e){const el=e.target.closest('.day-timeline .pf-time,.day-timeline .pf-label-input');if(!el)return;e.stopPropagation();el.focus({preventScroll:true});if(el.classList.contains('pf-time')&&typeof el.showPicker==='function'){try{el.showPicker()}catch{}}}document.addEventListener('pointerdown',focusInput,true);document.addEventListener('touchend',e=>{const el=e.target.closest('.day-timeline .pf-time,.day-timeline .pf-label-input');if(!el)return;e.stopPropagation();setTimeout(()=>{el.focus({preventScroll:true});if(el.classList.contains('pf-time')&&typeof el.showPicker==='function'){try{el.showPicker()}catch{}}},0)},true);new MutationObserver(unlock).observe(document.documentElement,{childList:true,subtree:true});unlock();})();