(()=>{'use strict';function prepare(el){if(!el)return;el.style.pointerEvents='auto';el.style.position='relative';el.style.zIndex='20';el.removeAttribute('readonly');el.removeAttribute('disabled');if(el.classList.contains('pf-time')){if(el.type!=='time')el.type='time';el.inputMode='numeric'}else{if(el.type!=='text')el.type='text';el.inputMode='text';el.autocomplete='off'}}function activate(e){const el=e.target.closest?.('.day-timeline .pf-time,.day-timeline .pf-label-input');if(!el)return;prepare(el);e.stopPropagation();try{el.focus({preventScroll:true})}catch{el.focus()}if(el.classList.contains('pf-time')&&typeof el.showPicker==='function'){try{el.showPicker()}catch{}}}document.addEventListener('pointerdown',activate,true);document.addEventListener('focusin',e=>{const el=e.target.closest?.('.day-timeline .pf-time,.day-timeline .pf-label-input');if(el)prepare(el)},true);document.querySelectorAll('.day-timeline .pf-time,.day-timeline .pf-label-input').forEach(prepare);})();